export type { IIconProps, IFormProps, ISettingQr } from '../commons/types';

export interface IData {
  concept: string;
  description: string;
  unitAmount: number;
  email: string;
  ownerId: string;
}
